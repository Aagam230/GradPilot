services:
  db:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: gradpilot
      POSTGRES_PASSWORD: gradpilot
      POSTGRES_DB: gradpilot
    ports: ["55432:5432"]
    volumes: ["pgdata:/var/lib/postgresql/data"]
volumes:
  pgdata:
